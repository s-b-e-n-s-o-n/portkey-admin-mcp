# Required: Portkey API Key (get from https://app.portkey.ai/settings/api-keys)
PORTKEY_API_KEY=your_portkey_api_key_here

# Optional: Transport mode (stdio or http, default: stdio)
# MCP_TRANSPORT=stdio

# Optional: HTTP server port (default: 3000, only used when MCP_TRANSPORT=http)
# MCP_PORT=3000

# Optional: HTTP server host (default: localhost, only used when MCP_TRANSPORT=http)
# MCP_HOST=localhost

# Optional: Session timeout in milliseconds (default: 3600000 = 1 hour)
# MCP_SESSION_TIMEOUT=3600000

# Optional: Allowed origins for CORS (comma-separated, default: http://localhost,https://localhost)
# ALLOWED_ORIGINS=http://localhost,https://localhost

# Optional: Enable rate limiting (default: false)
# RATE_LIMIT_ENABLED=false

# Optional: Max tokens for rate limiting (default: 100)
# RATE_LIMIT_MAX=100

# Optional: Rate limit window in milliseconds (default: 60000)
# RATE_LIMIT_WINDOW_MS=60000

# Optional: Rate limit refill rate (default: 10)
# RATE_LIMIT_REFILL=10
